import type { RunPlan } from "./harnessApi";
export function RunPlanPanel({ plan, onAdvance }: { plan: RunPlan; onAdvance: () => void }) { return <section aria-label="Coding harness run plan"><h2>Run-Plan: {plan.template_id}</h2><p>Status: {plan.status} · Schritt: {plan.current_step} · Versuch: {plan.attempt}</p><p>Gate: {plan.gate_status}</p><pre aria-label="Run diff">{plan.diff || "Kein Diff"}</pre><p>{plan.report}</p>{plan.status !== "succeeded" && plan.status !== "failed" && plan.status !== "cancelled" && <button type="button" disabled={plan.status === "awaiting_gate"} onClick={onAdvance}>Nächsten Schritt</button>}</section>; }
