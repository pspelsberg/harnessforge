import { fireEvent, render, screen } from "@testing-library/react";import { describe, expect, it, vi } from "vitest";import { HarnessCatalog } from "./HarnessCatalog";const t={template_id:"safe",version:"1.0.0",description:"<untrusted>",capabilities:["tests"],steps:[{step_id:"test",action:"test",requires_gate:false}]};describe("HarnessCatalog",()=>{it("shows capabilities and activation without HTML interpretation",()=>{const activate=vi.fn();render(<HarnessCatalog templates={[t]} onActivate={activate}/>);expect(screen.getByText("<untrusted>")).toBeTruthy();fireEvent.click(screen.getByRole("button",{name:"Read-only aktivieren"}));expect(activate).toHaveBeenCalledWith("safe");expect(screen.queryByRole("script")).toBeNull();});});
