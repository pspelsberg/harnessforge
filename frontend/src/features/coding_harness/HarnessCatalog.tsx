export function HarnessCatalog({templates,onActivate}:{templates:{template_id:string;version:string;description:string;capabilities:string[];steps:{step_id:string;action:string;requires_gate:boolean}[]}[];onActivate:(id:string)=>void}){return <section aria-label="Coding harness catalog"><h2>Coding-Harness-Vorlagen</h2>{templates.map(t=><article key={t.template_id}><h3>{t.template_id} v{t.version}</h3><p>{t.description}</p><p>Capabilities: {t.capabilities.join(", ")||"keine"}</p><ul>{t.steps.map(s=><li key={s.step_id}>{s.action}{s.requires_gate?" · Human Gate":""}</li>)}</ul><button type="button" onClick={()=>onActivate(t.template_id)}>Read-only aktivieren</button></article>)}</section>}
