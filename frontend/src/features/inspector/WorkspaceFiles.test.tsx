import {render,screen} from "@testing-library/react"; import {it,expect,vi} from "vitest"; import {WorkspaceFiles} from "./WorkspaceFiles"; it("lists bounded workspace files",async()=>{vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({files:["agents.md"]}),{status:200}));render(<WorkspaceFiles token="t" onSelect={vi.fn()}/>);expect(await screen.findByText("agents.md")).toBeTruthy();vi.restoreAllMocks();});