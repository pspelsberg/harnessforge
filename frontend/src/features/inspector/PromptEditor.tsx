import {useState} from "react"; export function PromptEditor({value,onChange}:{value:string;onChange:(value:string)=>void}){const [draft,setDraft]=useState(value);const update=(next:string)=>{if(new TextEncoder().encode(next).byteLength<=128*1024){setDraft(next);onChange(next)}};return <label>Node prompt<textarea aria-label="node prompt" value={draft} onChange={e=>update(e.target.value)} /></label>}