import {it,expect,vi} from "vitest"; import {requestProviderApproval} from "./approvalApi"; it("requests server approval",async()=>{vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({approval_fingerprint:"a".repeat(64),provider:"openai",bindings:["query"]})));expect((await requestProviderApproval({},["query"],"t")).approval_fingerprint).toHaveLength(64);vi.restoreAllMocks();});