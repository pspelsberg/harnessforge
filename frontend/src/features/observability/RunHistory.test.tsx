import {render,screen} from "@testing-library/react"; import {it,expect,vi} from "vitest"; import {RunHistory} from "./RunHistory"; it("lists runs from API",async()=>{vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({runs:[{id:"r1",created_at:"now"}]})));render(<RunHistory token="t" onSelect={vi.fn()} onDelete={vi.fn()}/>);expect(await screen.findByText("r1")).toBeTruthy();vi.restoreAllMocks();});