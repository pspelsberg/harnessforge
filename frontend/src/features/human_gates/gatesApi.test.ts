import { afterEach, describe, expect, it, vi } from "vitest";
import { getGate, decideGate } from "./gatesApi";
describe("human gate API", () => { afterEach(() => vi.restoreAllMocks()); it("binds decision and session IDs in authenticated requests", async () => { const fetchMock=vi.spyOn(globalThis,"fetch").mockImplementation(async () => new Response(JSON.stringify({contract_version:"1",request_id:"gate-1",nonce:"a".repeat(32),run_id:"run-1",node_id:"node",gate_class:"tool_write",action_fingerprint:"b".repeat(64),preview:{action:"tool.execute",command:"safe",diff:"",dataflow:"local",risk:"high",write_targets:[]},status:"pending"}),{status:200})); await getGate("gate/1","session-1","token"); await decideGate("gate/1",{request_id:"gate/1",nonce:"a".repeat(32),session_id:"session-1",decision:"denied"},"token"); expect(fetchMock.mock.calls[0][0]).toContain("gate%2F1"); expect(new Headers(fetchMock.mock.calls[1][1]?.headers).get("x-harnessforge-token")).toBe("token"); }); });
