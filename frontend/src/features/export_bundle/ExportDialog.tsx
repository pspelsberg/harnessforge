import {useState} from "react"; export function ExportDialog({onExport}:{onExport:()=>Promise<void>|void}){const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);const run=async()=>{setBusy(true);setError(null);try{await onExport()}catch{setError("Export failed")}finally{setBusy(false)}};return <section aria-label="export dialog">{error&&<p role="alert">{error}</p>}<button disabled={busy} onClick={run}>{busy?"Exporting…":"Export runner bundle"}</button></section>}