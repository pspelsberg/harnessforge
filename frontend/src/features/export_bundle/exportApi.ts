import {apiJson} from "../../shared/api"; import type {ForgeNode,ForgeEdge} from "../canvas/graphStore"; export type ExportGraph={schema_version:"1";id:string;name:string;workspace_path:string;nodes:ForgeNode[];edges:ForgeEdge[];settings:{review_only:boolean;external_dataflow_activated:boolean}}; export function exportGraph(graph:ExportGraph,destination:string,token:string){return apiJson<{files:string[]}>("/api/export",{method:"POST",token,body:JSON.stringify({graph,destination})})}