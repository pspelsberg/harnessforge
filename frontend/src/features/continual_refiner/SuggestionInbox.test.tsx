import { fireEvent, render, screen } from "@testing-library/react";import { describe, expect, it, vi } from "vitest";import { SuggestionInbox } from "./SuggestionInbox";
const suggestion={contract_version:"1" as const,suggestion_id:"suggestion-1",run_id:"run-1",session_id:"session-1",finding:{title:"<untrusted>",evidence:["evidence <text>"],risk:"medium"},patch:{path:"agents.md",diff:"-old\n+new",old_text:"old",new_text:"new"},status:"pending" as const,expires_at:"2099-01-01T00:00:00+00:00"};describe("SuggestionInbox",()=>{it("renders untrusted evidence as text and labels approval",()=>{render(<SuggestionInbox suggestions={[suggestion]} onApply={vi.fn(async()=>{})} onRollback={vi.fn(async()=>{})}/>);expect(screen.getByText("<untrusted>")).toBeTruthy();expect(screen.getByText("evidence <text>")).toBeTruthy();expect(screen.getByRole("button",{name:/Freigabe erforderlich/})).toBeTruthy();expect(screen.queryByRole("script")).toBeNull();});it("requires rollback callback",async()=>{const rollback=vi.fn(async()=>{});render(<SuggestionInbox suggestions={[{...suggestion,status:"applied"}]} onApply={vi.fn(async()=>{})} onRollback={rollback}/>);fireEvent.click(screen.getByRole("button",{name:"Rollback"}));expect(rollback).toHaveBeenCalledWith(expect.objectContaining({suggestion_id:"suggestion-1"}));});});
