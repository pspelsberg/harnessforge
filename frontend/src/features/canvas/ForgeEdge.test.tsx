import {render} from "@testing-library/react"; import {ReactFlowProvider} from "@xyflow/react"; import {it,expect} from "vitest"; import {ForgeEdge} from "./ForgeEdge"; it("renders an animated gradient edge when active",()=>{const props={id:"e",sourceX:0,sourceY:0,targetX:100,targetY:100,sourcePosition:"right" as const,targetPosition:"left" as const,data:{active:true}};const {container}=render(<ReactFlowProvider>{(ForgeEdge as unknown as (p:Record<string,unknown>)=>React.ReactElement)(props)}</ReactFlowProvider>);expect(container.querySelector("linearGradient")).toBeTruthy();expect(container.querySelector(".forge-edge-particle")).toBeTruthy();});