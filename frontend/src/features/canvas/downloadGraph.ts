import type {ForgeEdge,ForgeNode} from "./graphStore"; export function downloadGraph(graph:{schema_version:"1";nodes:ForgeNode[];edges:ForgeEdge[]},filename="graph.forge.json"){const blob=new Blob([JSON.stringify(graph,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const anchor=document.createElement("a");anchor.href=url;anchor.download=filename;anchor.click();URL.revokeObjectURL(url)}