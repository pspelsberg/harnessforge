import {apiJson} from "../../shared/api"; import type {ForgeNode,ForgeEdge} from "./graphStore"; export function saveGraph(path:string,graph:{schema_version:"1";id:string;name:string;workspace_path:string;nodes:ForgeNode[];edges:ForgeEdge[];settings:Record<string,unknown>},token:string){return apiJson<{path:string;review_only:boolean}>("/api/graph",{method:"POST",token,body:JSON.stringify({path,graph})})}