import {Handle,Position} from "@xyflow/react"; import type {NodeProps} from "@xyflow/react"; import type {NodeType} from "./graphStore"; import {NODE_LABELS,NODE_COLORS} from "./nodeRegistry"; export type ForgeVisualData={type:NodeType;label?:string;color?:string;status?:"idle"|"running"|"success"|"error"}; export function ForgeNode(props:NodeProps){const data=props.data as unknown as ForgeVisualData;const color=data.color||NODE_COLORS[data.type];return <article aria-label={`${NODE_LABELS[data.type]} node`} className={`forge-node forge-node-${data.status||"idle"}`} style={{borderColor:color}}><Handle type="target" position={Position.Left}/><strong>{data.label||NODE_LABELS[data.type]}</strong><span>{data.status||"idle"}</span><Handle type="source" position={Position.Right}/></article>}