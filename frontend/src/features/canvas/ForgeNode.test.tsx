import {render,screen} from "@testing-library/react"; import {ReactFlowProvider} from "@xyflow/react"; import {it,expect} from "vitest"; import {ForgeNode} from "./ForgeNode"; it("renders node status as text",()=>{const props={id:"x",data:{type:"llm",status:"running"},type:"default",dragging:false,zIndex:0,isConnectable:true,positionAbsoluteX:0,positionAbsoluteY:0};render(<ReactFlowProvider>{(ForgeNode as unknown as (props:Record<string,unknown>)=>React.ReactElement)(props)}</ReactFlowProvider>);expect(screen.getByText("running")).toBeTruthy();});