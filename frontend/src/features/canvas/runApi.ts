import {apiJson} from "../../shared/api"; import type {ForgeNode,ForgeEdge} from "./graphStore"; export function runGraph(graph:unknown,query:string,token:string){if(new TextEncoder().encode(query).byteLength>128*1024)throw new Error("query too large");return apiJson<{status:string;state:Record<string,unknown>}>("/api/run",{method:"POST",token,body:JSON.stringify({graph,query})})}