export function IndexStatusPanel({status,onPause,onResume}:{status:{status:string;version:number;indexed_files:number;queue_depth:number;last_sync:string|null;error:string|null};onPause:()=>void;onResume:()=>void}){return <section aria-label="Workspace index status"><h2>Workspace-Index</h2><p>Status: {status.status} · Version: {status.version} · Dateien: {status.indexed_files} · Queue: {status.queue_depth}</p><p>Letzte Synchronisierung: {status.last_sync||"noch nicht"}</p>{status.error&&<p role="alert">{status.error}</p>}{status.status==="paused"?<button type="button" onClick={onResume}>Fortsetzen</button>:<button type="button" onClick={onPause}>Pausieren</button>}</section>}
